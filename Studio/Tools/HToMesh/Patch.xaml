<UserControl x:Class="Skill.Studio.Tools.HToMesh.Patch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" x:Name="This" >
    <UserControl.Resources>
        <SolidColorBrush Color="#50FFFF00" x:Key="SelectedColor" />
    </UserControl.Resources>
    <Grid >
        <Rectangle Fill="Transparent" MouseMove="Img_MouseMove" MouseEnter="Img_MouseEnter" />
        <Border BorderBrush="{StaticResource SelectedColor}" BorderThickness="1" />
        <Rectangle Fill="{StaticResource SelectedColor}" Visibility="{Binding BorderVisibility, ElementName=This}" IsHitTestVisible="False" />        
    </Grid>
</UserControl>
