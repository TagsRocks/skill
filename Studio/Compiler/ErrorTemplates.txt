Project :
	Erros :
		1- Two node with same name.

AI:

	BehaviorTree :
		Errors :
			1- Two or more Behavior with same name.
			2- Invalid accesskey and address for AccessLimitDecorator.
			3- Invalid name of behavior node ( null or empty).
			4- zero or less than zero Weight of Behavior.
			5- Two or more Accesskeys with same name. (this must avoided in editor)						
			6- CounterLimitAccessKey with zero or less than zero MaxAccessCount.
			7- TimeLimitAccessKey with with zero or less than zero TimeInterval.
			8- Two parameters of a Behavior with same name (ignorecase).			

		Warnings :			
			1- Composite With no children.
			2- A Decorator with 'NeverFail' property setted to true is child of PrioritySelector. This cause next children unreachable.
			3- A Decorator with 'NeverFail' property setted to true is child of ConcurrentSelector with 'SuccessPolicy' property setted to 'SucceedOnOne'. This cause ConcurrentSelector never fail.

	SharedAccessKeys:
		Errors :
			1- Two or more Accesskeys with same name. (this must avoided in editor)
			2- Invalid name of Accesskey (null or empty).
			3- CounterLimitAccessKey with zero or less than zero MaxAccessCount.
			4- TimeLimitAccessKey with with zero or less than zero TimeInterval.

		Warnings :
			

Animation:
	
	AnimationTree :
		Errors :
			1- Invalid Skinmesh address for AnimationTree.
			2- Invalid name of AnimationTree Profile.
			3- Duplicate name in Profiles of AnimationTree.
			4- Empty Fromat of AnimationTree Profile (null or empty).
			5- Invalid name of AnimNode ( null or empty).
			6- Two or more AnimNodes with same name.
			7- Less than zero value for AnimNode BlendTime.
			8- More than one AnimNodeAimOffsetProfile with same name.
			9- Invalid name of AnimNodeAimOffsetProfile (null or empty).
			10- Invalid AnimNodeSequence for each child of AnimNodeAimOffsetProfile.			
			12- less than zero value for AnimNodeBlendBySpeed BlendUpTime,BlendDownTime,BlendUpDelay,BlendDownDelay,BlendDownPercent
			13- None acsendant Constaints of AnimNodeBlendBySpeed.
			14- less than zero value for AnimNodeOverride OverridePeriod.
			15- zero or less zero Chance of AnimNodeRandom child.
			16- UnAsigned AnimationClip of AnimNodeSequence.						
			17- Invalid name of AnimationClip (null or empty).						
			18- TreeAddress of AnimNodeSubTree is invalid

		Warnings :
			1- UnAssigned Connector ( at least two connector for BlendByPosture).
			2- AnimNode with no out Connection.

	SkinMesh :
		Errors :
			1- Two or more AnimationClips with same name in SkinMesh Animations.
			2- Two or more childs of a bone with same name.
			3- Invalid name of Bone (null or empty).
			4- Zero or less than zero value for Length of AnimationClip.

IO:

	SaveData :
		Errors :
			1- Invalid name of SaveProperty (null or empty).
			2- Two or more SaveProperty of one SaveClass with same name.			
			3- ClassProperty with invalid ClassName.
			4- Two or more SaveClasses With Same name;
			5- Invalid name of SaveClass (null or empty).

		Warnings :			
			1- SaveClass With no property.
			2- SaveData With no property.
