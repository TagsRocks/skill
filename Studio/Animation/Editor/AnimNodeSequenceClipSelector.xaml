<Window x:Class="Skill.Studio.Animation.Editor.AnimNodeSequenceClipSelector"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:anim="clr-namespace:Skill.Studio.Animation"
        xmlns:animedit="clr-namespace:Skill.Studio.Animation.Editor"
        Title="AnimationClip Selector" Height="700" Width="400" Background="{StaticResource WindowBg}" WindowStartupLocation="CenterOwner" >
    <Grid  >
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="38" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <animedit:AnimNodeSequenceClipSelectorControl x:Name="_Selector" />
        </Grid>
        <Grid Background="{StaticResource ButtonPanelBg}" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />                
            </Grid.ColumnDefinitions>
            <Button Content="Close" Width="90" VerticalAlignment="Center" Grid.Column="1"  Click="BtnClose_Click"/>            
        </Grid>
    </Grid>
</Window>
